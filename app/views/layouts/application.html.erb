<!DOCTYPE html>
<html>
  <head>
    <title>Importador de Contactos</title>
    <%= render 'layouts/head' %>
    <%= render 'layouts/shim' %>

  </head>

  <body class="hold-transition skin-blue sidebar-mini">
    <%= render 'layouts/header'%>
    <%= render 'layouts/menu'%>

    <div class="content-wrapper">
      <% flash.each do |message_type, message| %>
        <% if message == 'Has iniciado sesión satisfactoriamente.'%>
          <% next %>
        <% end %>
        <div class="alert alert-<%= message_type %>"><%= message.html_safe %></div>
      <% end %>
      <%= yield %>

    </div>
  </body>
</html>
